/* ===== Курсове (index.html #courses) ===== */
.courses__eyebrow {
    display: inline-block; font-size: .72rem; font-weight: 600; letter-spacing: .22em; text-transform: uppercase;
    color: var(--rose); position: relative; padding: 0 2.2rem;
}
.courses__eyebrow::before, .courses__eyebrow::after {
    content: ""; position: absolute; top: 50%; width: 1.5rem; height: 1px; background: var(--gold);
}
.courses__eyebrow::before { left: 0; } .courses__eyebrow::after { right: 0; }
.courses .section__head { margin-bottom: 3rem; }
.courses .section__head h2 { margin: .6rem 0 .9rem; }
.courses .section__head .lead { text-wrap: balance; }

.courses__grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 28px; }

.course-card {
    display: flex; flex-direction: column; background: var(--ivory);
    border: 1px solid var(--line); border-radius: var(--r-lg); overflow: hidden;
    box-shadow: var(--sh-sm);
    transition: opacity .7s var(--ease-out), transform .7s var(--ease-out), box-shadow .5s var(--ease-out), border-color .5s var(--ease-out);
}
.course-card__media { position: relative; margin: 0; aspect-ratio: 4 / 3; overflow: hidden; background: var(--blush-soft); }
.course-card__media img { width: 100%; height: 100%; object-fit: cover; display: block; transform: scale(1.02); transition: transform 1.1s var(--ease-out); }
.course-card__media::after {
    content: ""; position: absolute; inset: 0; pointer-events: none;
    background: linear-gradient(180deg, rgba(34,31,28,.28) 0%, rgba(34,31,28,0) 22%, rgba(34,31,28,0) 50%, rgba(34,31,28,.55) 100%);
}
.course-card__num {
    position: absolute; top: 14px; left: 16px; z-index: 1;
    font-family: 'Playfair Display', serif; font-style: italic; font-size: .95rem; color: #fff;
    text-shadow: 0 1px 8px rgba(0,0,0,.35); opacity: .9;
}
.course-card__price {
    position: absolute; right: 16px; bottom: 14px; z-index: 1;
    display: inline-flex; align-items: baseline; gap: .15rem;
    font-family: 'Playfair Display', serif; font-weight: 600; font-size: 2rem; line-height: 1; color: #fff;
    text-shadow: 0 2px 12px rgba(0,0,0,.3);
}
.course-card__price small { font-size: .55em; font-weight: 500; }
.course-card__body { display: flex; flex-direction: column; gap: .55rem; padding: 1.5rem 1.6rem 1.7rem; flex: 1; }
.course-card__body h3 { margin: 0; font-size: 1.35rem; line-height: 1.25; }
.course-card__body p { margin: 0; color: var(--ink-soft); font-size: .95rem; line-height: 1.6; }
.course-card__badge {
    align-self: flex-start; margin-top: .5rem;
    display: inline-flex; align-items: center; gap: .4rem;
    padding: .38rem .8rem; border-radius: var(--r-pill);
    background: var(--blush-soft); color: var(--rose-deep); font-size: .78rem; font-weight: 600;
}

@media (hover: hover) {
    .course-card:hover { transform: translateY(-6px); box-shadow: var(--sh-md); border-color: var(--gold-soft); }
    .course-card:hover .course-card__media img { transform: scale(1.08); }
}

.courses__cta {
    margin: 3rem auto 0; max-width: 760px;
    display: flex; align-items: center; justify-content: space-between; gap: 1.5rem;
    padding: 1.4rem 1.6rem 1.4rem 2rem; border-radius: var(--r-lg);
    background: var(--blush-soft); border: 1px solid var(--line);
}
.courses__cta-text { display: flex; flex-direction: column; gap: .2rem; }
.courses__cta-text strong { font-family: 'Playfair Display', serif; font-weight: 500; font-size: 1.25rem; color: var(--ink); }
.courses__cta-text span { color: var(--ink-soft); font-size: .95rem; }
.courses__call { gap: .6rem; flex: none; }
.courses__call-ic { display: inline-flex; animation: courseRing 3.2s ease-in-out infinite; transform-origin: 50% 60%; }
@keyframes courseRing {
    0%, 72%, 100% { transform: rotate(0); }
    76% { transform: rotate(-14deg); } 80% { transform: rotate(12deg); }
    84% { transform: rotate(-9deg); } 88% { transform: rotate(6deg); } 92% { transform: rotate(0); }
}

@media (max-width: 980px) {
    .courses__grid { grid-template-columns: repeat(2, 1fr); }
    .courses__grid > :last-child { grid-column: 1 / -1; max-width: calc(50% - 14px); justify-self: center; width: 100%; }
}
@media (max-width: 720px) {
    .courses .section__head { margin-bottom: 2rem; }
    .courses__grid { grid-template-columns: 1fr; gap: 18px; }
    .courses__grid > :last-child { max-width: none; }
    .course-card__media { aspect-ratio: 16 / 10; }
    .course-card__body { padding: 1.2rem 1.25rem 1.4rem; }
    .course-card__body h3 { font-size: 1.2rem; }
    .course-card__price { font-size: 1.75rem; }
    .courses__cta { flex-direction: column; text-align: center; padding: 1.5rem 1.25rem; margin-top: 2rem; gap: 1.1rem; }
    .courses__call { width: 100%; justify-content: center; }
}
@media (prefers-reduced-motion: reduce) {
    .courses__call-ic { animation: none; }
    .course-card, .course-card__media img { transition: none; }
}
